	letExp(
		decList(
			varDec(a,
				intExp(0),
				FALSE),
			decList()),
		seqExp(
			expList(
				forExp(i,
					intExp(0),
					intExp(100),
					seqExp(
						expList(
							assignExp(
								simpleVar(a),
								opExp(
									PLUS,
									varExp(
										simpleVar(a)),
									intExp(1))),
							expList(
								seqExp(
									expList()),
								expList()))),
					FALSE),
				expList())))